<% include ../partials/header %>

<div class="formContainer">

    <h1 class="ui centered header">Register!</h1>

    <form action="/charity/job" method="post" class="ui centered form">
      <div class="field">
        <label>Name</label>
        <input type="text" name="organization" placeholder="Title:" required>
      </div>
      <div class="field">
        <label>Charity Name</label>
        <input type="text" name="username" placeholder="Charity Name:" required>
      </div>
      <div class="field">
        <label>Charity Name</label>
        <input type="text" name="password" placeholder="Charity Name:" required>
      </div>
      <button class="ui green button" type="submit">Submit</button>
    </form>
</div>

<% include ../partials/footer %>